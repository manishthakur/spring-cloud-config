app:
  name: My App from config server
  description: Welcome to ${app.name} from confit server

my:
  greeting: Hello from config server
  list:
    values: One, Two, Three, Four | from config server

db:
    connection: connectin-string-from-config-server
    host: 0.0.0.1
    port: 1010